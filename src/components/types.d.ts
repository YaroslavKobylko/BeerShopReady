export interface Beer {
  id: number;
  name: string;
  tagline: string;
  first_brewed: string;
  description: string;
  image_url: string;
  abv: number;
  ibu: number;
  random?: boolean;
}

interface SurpriseBeer extends Beer {
  random: boolean;
}